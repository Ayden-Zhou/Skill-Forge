# Root Notes

- The repo root uses `uv` for Python environment and dependency management.
- Tooling required by `justfile` should be declared in project metadata when feasible so `just fmt` and `just check` are reproducible.
